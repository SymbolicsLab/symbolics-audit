---
session: 2026-07-07 — Modality Correspondence Study (Phase 0 → C + deep dives + Step-12)
type: handoff (research; NO vault edits — candidates only)
repos touched: symbolics-research (new `Modality Studies/`), developer-meta (OVERVIEW, theory-log, pending-integrations), claude-memory
companion thread: symbolics-agda (formal realization — machine-checked the axiom profile; carrying the L5+ "+")
rides on: `tension` clearing its entailment bar in the Inference study (upstream gate for the whole weld)
---

# Modality Correspondence Study — Session Handoff (2026-07-07)

The seventh Correspondence Study. Object: **the theory's L4 modal apparatus.** Built directly on the Inference
study. **NO vault edits** — research + candidates only. Deliverables in `symbolics-research/Modality Studies/`
(PLAN · Modality-in-the-Theory · 0.2-development · Cluster-List-and-Contrast-Class · General-Surveys-Raw ·
Correspondence-Survey-Consolidated · 6× deepdive-*). Companion formalization in `symbolics-agda`.

## The object (PB-pinned)
**Modality = the live, self-revising openness of a PROCESS; NO worlds.** Possibility is native, graded (by
resilience), and self-revising; **necessity = the derived high-resilience ceiling** of foreclosure (not
truth-in-all-worlds). Neither prior (contra the alethic *priority* of □; `◇≡¬□¬` is the concession-to-outsiders
form — in the native construction it is not even statable). Modality is the **L4 sibling of the Inference study's
L3 object** (elective/graded, one level up on *variation* instead of *relation*); stays L4 (the engine sets the
mode, is not the modality).

## Step-5 point (PB-named)
> **Necessity is not in the world — it's what maximal foreclosure makes of graded openness.**

## The session's central results
1. **The axiom profile is DERIVED, not chosen — and now MACHINE-CHECKED.** Settlement validates exactly **K, D, 4**
   and refutes **T, 5, B** (K+D+4 minus T,5,B). Derived theory-first (branching-time strict-future operator on
   directed/serial/transitive/irreflexive frames; SDL=KD="profile minus idempotence"; Mumford-Anjum ¬T+graded;
   paraconsistent-modal; Humean laws) AND independently machine-checked in `symbolics-agda` (checkpoint 4: operator
   built only from settlement+continuation, nothing aimed at the profile). **T and 5 = the two forcing axioms the
   theory is defined against.** **S5 = the maximal-hardening corner** (fixed world-space + reflection).
2. **The metabolic-signal weld (PB point, §4 of 0.2; the shared cross-study node).** Contradiction/tension is
   metabolism's *signal* — it makes remainder/intrusion actionable; acting on it at L4 IS the modal move. Joins
   Inference (L3 signal) ↔ Metabolism (maintenance face) ↔ Modality (L4 action); grounds T-failure (the metabolism
   never quiets) and 5-failure; reads crystallization/dissolution as signal-pathologies; **necessity = metabolic-quiet.**
   Logged to `.pending-integrations §3` (Agda-thread node; Modality candidates build on it).
3. **"No worlds" is a DRAW at truth-conditions (Pearl-Halpern) — repositioned as genealogy.** Worlds = the frozen
   snapshot of a process; supported by algebraic duality, Kochen-Specker contextuality, NL acquisition/diachrony
   priority, and the **stipulability proof** (active-row possibility has no cross-world state-form → modal realism =
   the stipulability error applied to possibility; derived from the vault's 2026-05-09 stipulability asymmetry).
   The extensional draw is provably an **artifact of acyclicity**; it breaks in the self-affecting/feedback (L5+) case.
4. **SELF-REVISION is the sole non-redundant claim — and it is L5+, not L4.** By-exhaustion (14 clusters + 6 dives):
   the field has dense homes for the L4 modality (object-internal / graded / native / no-worlds-at-core — Vetter,
   branching-time, potentiality, the virtual) but **none for self-revision** (everyone re-installs a fixed
   menu/space). **PB level-correction:** the "+" is L5+ (the self), so the L4 study's own contribution is a
   **derivation + a dissolution** (like Dependence); self-revision is its **L5+ payoff, handed off.**
5. **The "+" resolved (PB point 4, CONFIRMED).** The "+" = **contradiction *subsumed*, at the level of the self,
   regarding remainder/intrusion — the metabolic-signal move run REFLEXIVELY** (= the vault's plastic act /
   self-constitution-as-metabolism / release-and-bind / epiphany, now given its modal identity). **Dissolves the
   tu-quoque** the deep dives kept hitting: no fixed higher-order *menu* of revision-rules (Deleuze's virtual /
   Vetter's stock) — the "ground" is a single content-generating *operation*, and self-revision is that operation
   folding on its own products (a menu can't self-revise; a dynamic can → revision goes all the way down, no fixed
   floor because the dynamic isn't a floor).

## Deep dives (6; each with its own `deepdive-*.md`)
Fine-essence (SURVIVING/narrowed — wins artifact/coincidence cases, standoff on natural-kind essence; **closes via
the grounding court-move** — natural-kind essence un-bundles like Dependence's water/H₂O, only abstract essence is
the grade-1 residue held by anti-Platonism) · quantum-chance (SURVIVING → **NEUTRALITY not "minimal"**: don't make
the world-claim; objective chance if real is a mode of "fit," never a menu/rule; modality apparatus-internal either
way; imposition two-source does NOT transfer) · Vetter (ALLY — "L4 modality minus the +") · linear-graded-necessity
(BORROWABLE — `H_m^L` = a graded linear-exponential comonad `!_r`, grade = engine supply; crystallization = collapse
to unlimited `!`) · Pearl-Halpern (CORROBORATION — no-worlds is a truth-conditional draw; relocate to revisability;
feedback = where the fixed-space representation breaks) · Bergson-Deleuze (ALLY — the anti-menu template + the
tu-quoque residue self-revision must earn).

## Step-12 (PB injection): B + the imposed-possibility gap; A handed to L5
- **B (the L4 formalization frontier):** formalize the graded, engine-conditional L4 operator + local contesting.
  Homes: branching-time frame + graded comonad + paraconsistent base. Fresh FORMAL session (the Agda thread is on it).
- **The imposed-possibility gap (negative pole; renamed from "D" to avoid the axiom-D=seriality collision):**
  **fit/constraint in the modal register** — endogenous foreclosure (necessity you hold, by fit) vs exogenous
  foreclosure (necessity imposed on you, by constraint/apparatus/foe). Welds modality into the adversarial-second-body
  / imposition / constraint-as-multiplied-two-body / naturalization cluster. A genuine new structure.
- **A (formalize the "+"):** re-assigned to the L5/self work (feedback/non-well-founded/coalgebra home).

## Companion: the symbolics-agda thread
Machine-checked the profile (K+D+4 / ¬T¬5¬B; D-lost-at-crystallization proved; `◇≡¬□¬` not statable in the native
construction → the possibility-native inversion is **structural**). Reconciliations returned this session: **5-failure
is over-determined** — foreclosure fails it at L4 (possibility-set shrinks), generation fails it at L5+ (set grows) —
**confirming the level discipline from the formal side.** The Agda thread is carrying **A** (the L5+ "+"; currently at
the self-as-permanent-T-failure frame). Open ask relayed to PB: whether the "+" formal seed = the frame
(self-as-permanent-T-failure) or the **act** (reflexive metabolic subsumption on that frame; my rec = both, layered);
and the crown target = *exhibiting* the Pearl-Halpern break (proving a self-affecting continuation admits no
worlds-representation — the one place "no worlds" stops being a draw, at L5+).

## Discipline / status
- **NO vault edits.** All candidates ride on `tension` clearing its bar (Inference study); propagation = a fresh vault
  session. Citation-audit (Crossref) every deep-dive cite before external use (many flagged).
- **Publication candidate** (with Inference): the "elective/graded L-level policy" pair (L3 inference + L4 modality);
  a modal-realism dissolution + inversion.
- Full detail: `Modality Studies/Correspondence-Survey-Consolidated.md` (survey §0–10 + Step-12 §11); development in
  `0.2-development.md`; the shared weld + Modality candidates in `.pending-integrations §3`.
